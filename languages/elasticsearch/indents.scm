; Auto-indentation for Elasticsearch (.es) files.
;
; Indentation inside JSON request bodies is handled by the injected JSON
; grammar (see injections.scm), which carries its own indent rules. The .es
; grammar itself has no indentable constructs outside the body, so this file
; is intentionally minimal.
