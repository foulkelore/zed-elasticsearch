; Outline / symbol structure for Elasticsearch (.es) files.
;
; Each request becomes one outline entry, labeled by its method and path so
; you can navigate between requests (cmd-shift-o) and fold them.

(request
  (method) @name
  (path) @name) @item
